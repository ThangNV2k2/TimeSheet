import { ConvertMinuteToHoursPipe } from './convert-minute-to-hours.pipe';

describe('ConvertMinuteToHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMinuteToHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
